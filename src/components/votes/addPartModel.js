import React, { Component } from 'react';
import {
  Dialog,
  DialogTitle,
  DialogContent,
  DialogContentText,
  TextField,
  InputAdornment,
  Typography,
  Grid,
  Button,
  DialogActions,
} from '@material-ui/core';
import { Email } from '@material-ui/icons';

export class addPartModel extends Component {
  render() {
    return (
     
    );
  }
}

export default addPartModel;
